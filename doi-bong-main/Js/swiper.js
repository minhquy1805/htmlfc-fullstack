    const swiper = new Swiper('.swiper-container', {
    // Cài đặt cơ bản
    loop: true, // Lặp lại các slide

    autoplay: {
    delay: 5000, // Tự động chuyển slide mỗi 5 giây
},

    grabCursor: true, // Hiển thị con trỏ kéo (hình bàn tay)
    effect: 'slide', // Hiệu ứng trượt mặc định
    speed: 600, // Tốc độ chuyển đổi slide (ms)
    });

